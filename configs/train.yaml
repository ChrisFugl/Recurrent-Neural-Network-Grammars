defaults:
  - type: gen
  - model: ${defaults.0.type}.rnng
  - iterator: unordered
  - loader: wsj.${defaults.0.type}.oracle
  - loss: negative_action_log_likelihood
  - optimizer: adam
  - lr_scheduler: constant
  - stopping_criterion: manual
  - checkpoint: never
  - evaluator: batch
name: tmp
load_checkpoint: null
gpu: null
seed: null
hydra:
  run:
    dir: ./outputs/train/${name}/${now:%Y-%m-%d_%H-%M-%S}
